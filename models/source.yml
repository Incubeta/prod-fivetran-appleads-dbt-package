version: 2
sources:
  - name: apple
    schema: "{{ var('apple_schema', target.dataset) }}"
    tables:
      - name: appleads_adgroup_performance_v_1
        identifier: "{{ var('appleads_adgroup_performance_v_1_identifier', 'appleads_adgroup_performance_v_1') }}"
      - name: appleads_keyword_performance_v_1
        identifier: "{{ var('appleads_keyword_performance_v_1_identifier', 'appleads_keyword_performance_v_1') }}"
      - name: ad_group_history
        identifier: "{{ var('ad_group_history_identifier', 'ad_group_history') }}"
      - name: keyword_report
        identifier: "{{ var('keyword_report_identifier', 'keyword_report') }}"
      - name: country_or_region_history
        identifier: "{{ var('country_or_region_history_identifier', 'country_or_region_history') }}"
      - name: targeting_dimensions_history
        identifier: "{{ var('targeting_dimensions_history_identifier', 'targeting_dimensions_history') }}"
      - name: campaign_history
        identifier: "{{ var('campaign_history_identifier', 'campaign_history') }}"
      - name: organization
        identifier: "{{ var('organization_identifier', 'organization') }}"
      - name: search_term_report
        identifier: "{{ var('search_term_report_identifier', 'search_term_report') }}"
      - name: keyword_history
        identifier: "{{ var('keyword_history_identifier', 'keyword_history') }}"
